# Normalize all text files to LF in the repo and on checkout, so submodule
# clones (and Windows checkouts) don't introduce CRLF.
* text=auto eol=lf

# Treat common binaries as binary (no EOL conversion).
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary
*.svg text eol=lf
